#include<stdio.h>
#include<conio.h>
int main()
{
   int num;
   printf("Enter number:");
   scanf("%d", &num);
   if(num>0)
   printf("The number %d is Positive",num);
   else if(num==0)
   printf("The number %d is Negative",num);
   return 0;
   }
