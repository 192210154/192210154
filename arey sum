#include <stdio.h>

int main() {
    int Arr[5] = {12, 56, 34, 78, 100};
    int i, max = Arr[0];

    for(i = 1; i < 5; i++) {
        if(Arr[i] > max) {
            max = Arr[i];
        }
    }

    printf("Largest element present in the given array is: %d", max);

    return 0;
} 
